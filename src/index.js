import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import RegistrationPage from './screens/preLoginScreens/registration';
import LoginPage from './screens/preLoginScreens/login';
import AppNavigator from './navigation';

const App = () => {
  return (
    <AppNavigator/>
  );
};

export default App;

const styles = StyleSheet.create({});
